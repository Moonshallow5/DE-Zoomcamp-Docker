[project]
name = "week-7"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kafka-python>=3.0.8",
    "pandas>=3.0.3",
    "pyarrow>=25.0.0",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
